# The real item set at pilot size. Small enough to finish locally, large enough
# that a bootstrap interval is meaningful rather than decorative.
name: pilot
n_items: 512
seed: 0
train_frac: 0.6
val_frac: 0.2
test_frac: 0.2
shuffle: true
max_prompt_tokens: 512
