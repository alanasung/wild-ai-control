# Tiny memory ceiling and no resume, so a debugging run never picks up a stale
# cached artifact and never quietly starts swapping.
profile: PILOT
seed: 0
deterministic: true
resume: false
max_memory_gb: 4.0
allow_unsupported_hardware: false
save_git_sha: true
save_resolved_config: true
